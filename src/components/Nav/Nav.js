import React from 'react';
import "./Nav.scss"

const Nav = () => {

  return(
    <section className="nav-section">
      <button>Home</button>
      <button>View Favorites</button>
    </section>
  )
}

export default Nav;
